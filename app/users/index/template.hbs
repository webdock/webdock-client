<h2>Users</h2>

<table>
  <tr>
    <th>#</th>
    <th>Email</th>
  </tr>
  {{#each model as |user|}}
  <tr>
    <td>{{user.id}}</td>
    <td>{{user.email}}</td>
  </tr>
  {{/each}}
</table>

{{outlet}}
